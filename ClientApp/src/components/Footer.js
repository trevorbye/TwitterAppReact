import React from 'react';

export default function Footer(props) {
    return (
        <footer className="container-fluid">
            <nav className="navbar fixed-bottom">
                <i className="fab fa-react fa-4x mx-auto"></i>
            </nav>
        </footer>
    );
}

